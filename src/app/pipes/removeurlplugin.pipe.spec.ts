import { RemoveurlpluginPipe } from './removeurlplugin.pipe';

describe('RemoveurlpluginPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveurlpluginPipe();
    expect(pipe).toBeTruthy();
  });
});
