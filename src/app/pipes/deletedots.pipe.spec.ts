import { DeletedotsPipe } from './deletedots.pipe';

describe('DeletedotsPipe', () => {
  it('create an instance', () => {
    const pipe = new DeletedotsPipe();
    expect(pipe).toBeTruthy();
  });
});
