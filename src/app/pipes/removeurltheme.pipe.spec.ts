import { RemoveurlthemePipe } from './removeurltheme.pipe';

describe('RemoveurlthemePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveurlthemePipe();
    expect(pipe).toBeTruthy();
  });
});
